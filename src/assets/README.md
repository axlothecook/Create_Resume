# assets

Vite-bundled static assets imported from code.

- `auth-bg.jpg` — background photo for the auth (login/signup) screen, imported in
  `src/auth/AuthScreen.jsx`. **Drop your image here named exactly `auth-bg.jpg`.**
  If you use a different name/extension, tell me and I'll update the import path.
